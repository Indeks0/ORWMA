<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.topgamesapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splatoon2DetailsActivity"></activity>
        <activity android:name=".LolDetailsActivity" />
        <activity android:name=".PubgDetailsActivity" />
        <activity android:name=".HearthstoneDetailsActivity" />
        <activity android:name=".WebActivity" />
        <activity android:name=".FortniteDetailsActivity" />
        <activity android:name=".RecyclerConfigs" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>